// Tuỳ chọn số bản ghi mỗi trang — dùng chung cho các màn admin (đồng bộ label "X/trang").
export const ADMIN_PER_PAGE_OPTIONS = [5, 10, 20, 50];

export const adminPerPageLabel = (n) => `${n}/trang`;
